{
    "$id": "GPS",
    "type": "object",
    "properties": {
        "appId": {
            "type": "string",
            "const": "GPS"
        },
        "messageType": {
            "type": "string",
            "enum": [
                "HELLO",
                "DATA"
            ]
        },
        "data": {
            "type": "string",
            "pattern": "^\\$(GPGGA|GPGLL|GPGSA|GPGSV|GPRMC|GPVTG)(,[0-9A-Z\\*\\.]*)+$"
        }
    },
    "required": [
        "appId",
        "messageType"
    ],
    "additionalProperties": false
}
