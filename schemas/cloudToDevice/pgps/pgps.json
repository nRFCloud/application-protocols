{
   "title":"PGPS Response",
   "description":"Responds with link to PGPS file",
   "type":"array",
   "prefixItems": [
      { 
         "type": "string", 
         "desription": "Host. The protocol agnostic URL for accessing PGPS prediction files.",
         "pattern": "pgps\\.nrfcloud\\.com"
      },
      { 
         "type": "string", 
         "desription": "Path. The path to the file that contains PGPS prediction data.",
         "pattern": "public\\/.+.bin"
      }
   ]
}
